# GG-Beauty Codex 开发规则

本文件适用于本项目中的所有 Codex 代理及其子代理。除非项目所有者明确批准例外，所有开发、检查、测试、数据库和部署工作都必须遵守以下规则。

## 稳定基线与核心功能

1. 当前 `v1.0.0` 是稳定基线。不得无理由破坏现有能够正常工作的功能或改变既有业务行为。
2. 以下核心功能必须始终保持可用：
   - 顾客预约
   - 老板查看预约
   - 员工时间防撞单
   - 老板登录
   - 确认、完成和取消预约
3. 开发新功能或修复问题前，必须先检查现有实现。应优先采用小范围、可审查的修改，不得随意重构正常工作的代码。

## 历史业务数据保护

4. 禁止硬删除历史业务数据：
   - 预约取消必须使用 `cancelled` 状态，不得删除预约记录。
   - 员工离职必须使用停用状态，不得删除有历史记录的员工。
   - 服务下架必须使用停用状态，不得删除有历史记录的服务。
   - 顾客历史记录原则上必须保留。
5. UI 可以重构，但业务数据、历史记录以及现有数据关系必须保持兼容。

## 数据库安全与变更

6. 禁止执行 `DROP TABLE`、`TRUNCATE` 或其他可能破坏生产数据的数据库操作，除非项目所有者事先明确批准具体操作和范围。
7. 数据库结构变更必须通过可审查、可追踪的 migration 或 SQL 文件完成，并在执行前说明影响范围、兼容性风险和回滚方案。
8. 未经明确要求，不得修改生产 Supabase 数据。
9. 防撞单最终必须由数据库层提供并发安全保障，不得只依赖前端检查或普通的“先查询、再插入”逻辑。

## 安全、认证与数据隔离

10. 不得把密码、`DATABASE_URL`、API Key、Secret 或其他敏感凭证写入代码或提交到 GitHub。敏感配置必须通过受保护的环境变量或 Secret 管理机制提供。
11. 多店铺数据必须严格按 `shop_id` 和适用的 `location_id` 隔离。任何查询、写入、更新和权限校验都不得跨店铺或跨地点读取、泄露或修改数据。
12. 所有老板端敏感 API 必须执行真正的服务端认证与授权，不能只依靠前端登录遮罩、隐藏按钮或其他客户端限制。

## 架构与时间规则

13. 新功能应优先支持未来的多租户 SaaS 架构，不得无必要地写死单一店铺、员工、地点或租户数据。
14. `Asia/Singapore` 是当前默认业务时区。预约、营业时间、日期统计和状态时间戳相关逻辑必须明确且一致地处理该时区。

## 任务执行与交付

15. 未经项目所有者明确要求，不得自动执行以下操作：
    - 创建 Git commit
    - push 到 GitHub 或其他远端
    - 部署到 Render
    - 修改生产 Supabase 数据
16. 每个任务完成后，必须清楚说明：
    - 修改了哪些文件
    - 为什么修改
    - 如何测试或验证
    - 是否涉及数据库
    - 是否存在兼容性风险

